SELECT * FROM song;
-- Used wildcard * to SELECT/Show all records from 'song' database table (This way will show 'id' column as well NOT enumerated)
-- Side Note: SQL keywords are case-insensitive, i.e. FROM == fRom == from etc.